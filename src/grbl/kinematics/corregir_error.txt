Walkthrough: Debugging grblHAL Simulator Crash
Problema
El simulador grblHAL crasheaba inmediatamente al iniciar (exit code 1) tras integrar cinemática RTCP de 5 ejes.

Causa Raíz
Recursión infinita en 
transform_steps_to_cartesian
:

KINEMATICS_API
rtcp.c línea 715
∞ loop
Stack overflow → CRASH
sync_position()
system_convert_array_steps_to_mpos()
kinematics.transform_steps_to_cartesian()
💥 Exit code 1
La función 
transform_steps_to_cartesian
 en 
rtcp.c
 llamaba a 
system_convert_array_steps_to_mpos()
 para convertir steps→mm. Pero con KINEMATICS_API definido, esa función redirige a kinematics.transform_steps_to_cartesian() — la misma función → stack overflow.

Corrección
diff
static float *transform_steps_to_cartesian(float *position, int32_t *steps)
 {
     float mpos[N_AXIS];
-    system_convert_array_steps_to_mpos(mpos, steps);  // ← RECURSIÓN INFINITA
+    uint_fast8_t idx = N_AXIS;
+    do {
+        idx--;
+        mpos[idx] = steps[idx] / settings.axis[idx].steps_per_mm;
+    } while(idx);  // ← Conversión directa, sin recursión

acciones a realizar:
1. en rtcp.c realiza esto FIX: Conversión directa steps→mm en transform_steps_to_cartesian
2. en nvs.h realiza esto NVS_SIZE aumenta a 4096
3. en grbllib.c realiza esto RTCP init antes de nvs_buffer_init